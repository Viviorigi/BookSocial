server:
  port: 8085
  servlet:
    context-path: /chat
spring:
  application:
    name: chat-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27018/chat-service?authSource=admin

app:
  services:
    profile:
      url: http://localhost:8081/profile
